[
  {
    "name": "/help lists available commands",
    "steps": [
      {
        "send": { "text": "/help" },
        "expect": [
          { "method": "sendMessage", "payload": { "text": "Available commands:\n/start — Start the bot\n/help — Show this help message" } }
        ]
      }
    ]
  },
  {
    "name": "unknown command replies with friendly fallback",
    "steps": [
      {
        "send": { "text": "/nonexistent" },
        "expect": [
          { "method": "sendMessage", "payload": { "text": "Unknown command. Try /help to see what I can do." } }
        ]
      }
    ]
  },
  {
    "name": "plain text message replies with generic fallback",
    "steps": [
      {
        "send": { "text": "hello" },
        "expect": [
          { "method": "sendMessage", "payload": { "text": "I didn't understand that. Try /start to see what I can do." } }
        ]
      }
    ]
  }
]