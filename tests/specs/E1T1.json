[
  {
    "name": "/ping replies with pong and increments counter",
    "steps": [
      {
        "send": { "text": "/ping" },
        "expect": [
          { "method": "sendMessage", "payload": { "text": "pong 🏓" } }
        ]
      },
      {
        "send": { "text": "/ping" },
        "expect": [
          { "method": "sendMessage", "payload": { "text": "pong 🏓" } }
        ]
      },
      {
        "send": { "callback": "cmd:count" },
        "expect": [
          { "method": "answerCallbackQuery" },
          { "method": "sendMessage", "payload": { "text": "Total pings served: 2" } }
        ]
      }
    ]
  }
]
